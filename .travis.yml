language: java

sudo: false
install: true

jdk:
  - oraclejdk8

cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'
    - '$HOME/.sonar/installs'

addons:
  sonarqube: true

notifications:
  email: false

addons:
  sonarcloud:
    organization: "AT-NicolasMetivier"
    token:
      secure: CnessPluginSonarFramaC  # encrypted value of your token
# Script to execute.

script:
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=at-nicolasmetivier-github -Dsonar.login=d346d75a4f5ef46a91607cbce4bfc7227ee03841
